import { OnInit } from '@angular/core';
import { Router } from '@angular/router';
import { AuthenticationService } from '../auth.service';
export declare class AuthIconComponent implements OnInit {
    private router;
    private authService;
    private loginPageUri;
    popup: boolean;
    popupStyle: any;
    userName: string;
    userNameShort: string;
    constructor(router: Router, authService: AuthenticationService, loginPageUri: string);
    ngOnInit(): void;
    toggle(event: any): void;
    closePopup(): void;
    isAuthorized(): boolean;
    login(): void;
    logout(): void;
}
