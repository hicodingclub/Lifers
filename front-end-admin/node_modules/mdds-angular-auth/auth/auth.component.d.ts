import { AuthenticationService } from './auth.service';
export declare class AuthenticationComponent {
    private authenticationService;
    private authenticationInterfaces;
    adminInterface: boolean;
    userIntEnabled: boolean;
    adminIntEnabled: boolean;
    constructor(authenticationService: AuthenticationService, authenticationInterfaces: string);
    setAdminInterface(adminInterface: boolean): void;
}
