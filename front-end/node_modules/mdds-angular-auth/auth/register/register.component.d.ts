import { OnInit } from '@angular/core';
import { Router, ActivatedRoute } from '@angular/router';
import { FormBuilder, FormGroup } from '@angular/forms';
import { AuthenticationService } from '../auth.service';
export declare class RegisterComponent implements OnInit {
    private formBuilder;
    private router;
    private route;
    private authenticationService;
    registerForm: FormGroup;
    loading: boolean;
    submitted: boolean;
    servererror: boolean;
    serverText: string;
    constructor(formBuilder: FormBuilder, router: Router, route: ActivatedRoute, authenticationService: AuthenticationService);
    ngOnInit(): void;
    readonly f: {
        [key: string]: import("@angular/forms").AbstractControl;
    };
    onSubmit(): void;
    cancel(): void;
}
