import { HttpClient, HttpErrorResponse } from '@angular/common/http';
import { Observable } from 'rxjs';
import { Router } from '@angular/router';
export declare class AuthenticationService {
    private authServerRootUri;
    private router;
    private http;
    private interruptedUrl;
    private routedFromUrl;
    private previousUrl;
    private currentUrl;
    private navigateEndTime;
    private adminInterface;
    constructor(authServerRootUri: string, router: Router, http: HttpClient);
    isAuthorized(): boolean;
    private getLogoutTime;
    getAccessToken(): string;
    refreshToken(): Observable<any>;
    refreshShouldHappen(response: HttpErrorResponse): boolean;
    verifyTokenRequest(url: string): boolean;
    getInterruptedUrl(): string;
    getRoutedFromUrl(): string;
    setInterruptedUrl(url: string): void;
    getUserName(): string;
    login(userName: string, password: string): Observable<any>;
    register(userInfo: any): Observable<any>;
    loggedIn(user: any): any;
    logout(): void;
    setAdminInterface(isAdminInterface: boolean): void;
    isAdminInterface(): boolean;
}
